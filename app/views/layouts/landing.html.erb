<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="user-scalable=yes, width=device-width, initial-scale=1.0 maximum-scale=1.0" >
    <link href="https://fonts.googleapis.com/css?family=Lato|Raleway" rel="stylesheet">
    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->

    <title>Callista Consulting - <%= content_for?(:title) ? content_for(:title) : "create your business with us!" %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
<%= render partial: 'shared/navbar_fixed_top' %>
<%= render partial: 'shared/headerwrap' %>

    <div class="container w">
      <div class="row centered">
        <br><br>
        <div class="col-lg-4">
          <i class="far fa-handshake fa-2x"></i>
          <h4><%= t('landing_page.services.consulting_services.title') %></h4>
          <p><%= t('landing_page.services.consulting_services.desc') %></p>
        </div>
        <div class="col-lg-4">
          <i class="fa fa-home fa-2x"></i>
          <h4><%= t('landing_page.services.realty.title') %></h4>
          <p><%= t('landing_page.services.realty.desc') %></p>
        </div>
        <div class="col-lg-4">
          <i class="fa fa-laptop fa-2x"></i>
          <h4><%= t('landing_page.services.practical_seminars.title') %></h4>
          <p><%= t('landing_page.services.practical_seminars.desc') %></p>
        </div>
      </div>
      <br><br>
    </div>

    <div id="dg">
      <div class="container">
        <div class="row centered">
          <h4><%= t('landing_page.last_projects') %></h4>
          <br>
          <% if !@last_projects_decorators.blank? %>
            <% @last_projects_decorators.each do |project_decorator| %>
              <div class="col-lg-4 col-md-4">
                <div>
                  <%= link_to project_path(project_decorator.id) do %>
                    <figure class="imghvr-shutter-in-out-diag-1">
                        <%= project_decorator.landing_page_thumbnail_image %>
                      <figcaption>
                        <%= project_decorator.title %>
                      </figcaption>
                    </figure>
                  <% end %>
                </div>
              </div>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>

    <div class="container wb">
      <div class="row centered">
        <div class="col-lg-6 col-lg-offset-3">
          <h4><%= t('landing_page.we_create_your_business.title') %></h4>
          <p><%= t('landing_page.we_create_your_business.desc_html') %></p>
        </div>
        <div class="col-lg-8 col-lg-offset-2">
          <%= image_tag 'munten.png', class: "img-responsive", alt: "We create your business" %>
        </div>
      </div>
    </div>

    <div id="lg">
      <div class="container">
        <div class="row centered">
          <h4><%= t('landing_page.realty') %></h4>
          <% if !@last_realties_decorators.blank? %>
            <% @last_realties_decorators.each do |realty_decorator| %>
              <div class="col-lg-4 col-md-4">
                <div>
                  <%= link_to country_realty_path(realty_decorator.country, realty_decorator.id)  do %>
                    <figure class="imghvr-shutter-in-out-diag-1">
                      <%= realty_decorator.landing_page_image_thumbnail %>
                      <figcaption>
                        <%= realty_decorator.title %>
                      </figcaption>
                    </figure>
                  <% end %>
                </div>
              </div>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>

    <%= render partial: 'shared/footer' %>
  </body>
</html>
