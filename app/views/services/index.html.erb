<div id="breadcrambs">
  <div class="container">
    <ol class="breadcrumb">
      <li><%= link_to 'Главная', root_path %></li>
      <li><%= link_to 'Услуги', services_path %></li>
    </ol>
  </div>
</div>
<%= flash_messages %>
<div class="container services_list">
  <div class="row">
    <% @service_decorators.each do |service_decorator| %>
      <div class="col-md-4">
        <div class="card mb-4">
          <%= service_decorator.service_preview_img_thumbnail %>
          <div class="card-body">
            <p class="card-text" style="height: 84px; overflow: hidden">
              <%= service_decorator.preview %>
            </p>
            <div class="centered">
              <div class="btn-group">
                <%= service_decorator.link_to_show %>
                <%= service_decorator.link_to_edit %>
                <%= service_decorator.link_to_destroy %>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
  <hr>
  <%= link_to 'New Service', new_service_path, class: "btn btn-sm btn-outline-danger" if user_signed_in? %>
</div>

